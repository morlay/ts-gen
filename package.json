{
  "private": true,
  "scripts": {
    "exec": "lerna exec --",
    "clean": "npm run exec -- rm -rf lib",
    "clean:js": "npm run exec -- rm -rf \"src/{,**/}{,**/}*.js\"",
    "ava": "npm run exec -- ava -v",
    "build": "npm run exec -- tsc",
    "test": "npm run lint && npm run build && npm run ava",
    "lint": "tslint -c tslint.json --fix packages/**/src/{,**/}{,**/}*.ts{,x}"
  },
  "devDependencies": {
    "@types/node": "8.0.x",
    "ava": "0.22.x",
    "lerna": "2.1.x",
    "tslint": "5.7.x",
    "typescript": "2.5.x"
  }
}
